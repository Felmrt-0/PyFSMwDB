from setuptools import find_packages, setup
setup(
    name='FSM',
    packages=find_packages(include=['finite-state_machine_lib']),
    version='0.1.0',
    description='My first Python library',
    author='Me',
    license='ME',
    install_requires=[],
    setup_requires=[],
    tests_require=[],
    test_suite= '',
)